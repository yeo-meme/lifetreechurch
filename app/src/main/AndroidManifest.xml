<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.uncode.lifetreechurch">


    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".base.BaseApplication"
        android:allowBackup="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".lt_main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".fm_news.MemberDetailsActivity"/>
        <activity android:name=".fm_notice.NoticeDetailsActivity"/>
        <activity android:name=".fm_video.YoutubePlayerActivity"/>
        <activity android:name=".fm_happy.HappyActivity"/>
        <activity android:name=".IntroduceBottomActivity.IntroduceActivity"/>
        <activity android:name=".video_bottom_menu.IntroduceChActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"/>
        <activity android:name=".fm_happy.HappyWebActivity"/>

    </application>

</manifest>